DB_BACKEND=pg
PG_USER=pguser
PGPASSWORD=REPLACE_ME
PG_DATABASE=3onprime
PG_HOST=localhost
PG_PORT=5433
LOG_LEVEL=info
### Example environment variables for local development
# Database connection (used by migration runner and CLI)
DB_HOST=127.0.0.1
DB_PORT=5433
DB_USER=pguser
DB_PASSWORD=REPLACE_ME
DB_NAME=3onprime

# Admin account (used by scripts/set_admin_password.js)
#ADMIN_EMAIL=admin@localhost
#ADMIN_PASSWORD=REPLACE_ME

# FILE_ENCRYPTION_KEY - set in CI/production
#FILE_ENCRYPTION_KEY=replace-with-secure-key
# 3ONPRIME environment example
# Copy to .env and set secure values before running
PORT=3000
NODE_ENV=production
SESSION_SECRET=replace-with-a-secure-secret
MONGODB_URI=mongodb://mongo:27017/3onprime

# Agent proxy (optional) - leave blank to use mock responses
AGENT_PROVIDER=
AGENT_API_KEY=
AGENT_MODEL=gpt-4o-mini

# Agent proxy port (docker-compose maps 3001)
AGENT_UPLOAD_URL=http://localhost:3001/upload

# Execution guard (disabled by default) - enable only if you trust inputs
ALLOW_EXEC=false
COMMAND_WHITELIST=echo,ls,cat

# Paths (if running outside docker, adjust as needed)
DATA_DIR=./data
UPLOADS_DIR=./uploads
LOGS_DIR=./logs
